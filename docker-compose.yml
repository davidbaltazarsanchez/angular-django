services:
  backend:
    build: ./backend
    volumes:
      - ./backend:/app
    expose:
      - "8000"
    env_file:
      - .env

  nginx:
    build:
      context: .
      dockerfile: nginx/Dockerfile
    container_name: nginx_proxy
    ports:
      - "8089:80"
    depends_on:
      - backend
